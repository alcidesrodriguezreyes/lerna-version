{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "version:pre": "lerna version --conventional-prerelease --preid test --no-changelog --yes",
    "publish": "lerna publish from-git --yes"
  },
  "dependencies": {},
  "devDependencies": {
    "lerna": "^8.2.1"
  }
}
